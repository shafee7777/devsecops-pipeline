name: IaC Scan (placeholder)

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  iac-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Placeholder IaC scan
        run: |
          echo "IaC scanning pipeline ready."
          echo "Add Terraform/Kubernetes configs and integrate Checkov or tfsec here."
